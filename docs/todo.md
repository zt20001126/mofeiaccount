# Todo List - 简易本地记账软件

基于 [PRD](docs/prd.md) 和 [TDD](docs/tdd.md) 拆解的实现任务列表。

---

## Phase 1: 项目初始化与基础架构

- [ ] **1.1** 使用 Tauri 脚手架创建项目，配置 Vue3/React + TypeScript 技术栈
- [ ] **1.2** 安装所需依赖：`xlsx` (SheetJS)、`@tauri-apps/plugin-dialog`、`@tauri-apps/plugin-fs`
- [ ] **1.3** 定义核心 TypeScript 类型：`AccountRecord` 接口 (8 个字段) 和 `AppConfig` 接口 (`src/types/account.ts`)
- [ ] **1.4** 实现配置文件读写工具：保存/读取 `workDir` 到应用本地沙盒，支持首次启动检测

---

## Phase 2: 结余计算核心算法

- [ ] **2.1** 实现 `recalculateBalances()` 纯函数：根据全量账目列表按时间正序滚动计算每笔结余 (`balance = 上笔结余 + 本次收入 - 本次支出`)
- [ ] **2.2** 编写结余计算的单元测试 (覆盖 UT-001 ~ UT-004)

---

## Phase 3: Excel 读写服务

- [ ] **3.1** 实现 Excel 读取服务：将 `账本.xlsx` 文件解析为 `AccountRecord[]` 数组
- [ ] **3.2** 实现 Excel 写入服务：将 `AccountRecord[]` 数组整体覆写为 `账本.xlsx`，包含异常处理 (Try-Catch) 防止文件损坏
- [ ] **3.3** 实现账目 ID 生成器：格式 `TX + YYYYMMDD + 4位自增序号`（如 `TX202606060001`），每次新增时根据当天已有记录数自动递增

---

## Phase 4: 目录初始化与配置管理

- [ ] **4.1** 实现首次启动流程：检测 `workDir` 配置是否存在，不存在则弹出系统文件夹选择框
- [ ] **4.2** 选定目录后自动创建 `账本.xlsx`（含表头行）和 `/vouchers/` 文件夹
- [ ] **4.3** 保存所选目录路径到应用本地配置，下次启动直接加载
- [ ] **4.4** 已有配置的场景：启动后直接读取 `账本.xlsx` 并展示历史账目列表 (集成测试 IT-001)

---

## Phase 5: 新增记账模块

- [ ] **5.1** 实现新增记账表单页面 UI（客户名称、收款金额、支出金额、说明备注、凭证上传按钮）
- [ ] **5.2** 实现收款/支出金额输入时的实时结余预览联动（前端根据上笔结余 + 本次输入动态计算）
- [ ] **5.3** 实现表单校验：客户名称必填、收款/支出为合法数字且 >= 0（集成测试 IT-003）
- [ ] **5.4** 实现"选择凭证"按钮：调用系统文件选择器，限制 `.jpg` / `.png` 格式
- [ ] **5.5** 实现"保存"逻辑：
  - 生成账目 ID
  - 凭证图片复制到 `./vouchers/` 并重命名为 `[账目ID].png`（集成测试 IT-002）
  - 新记录追加到 `AccountRecord[]`，重新计算全量结余
  - 整体写入 `账本.xlsx`，刷新前端列表
  - 保存时显示 Loading 遮罩防重复点击（集成测试 IT-004）

---

## Phase 6: 查看与修改账目模块

- [ ] **6.1** 实现主界面历史账目表格展示（分页或滚动），包含所有 8 个字段列
- [ ] **6.2** 实现凭证列：点击缩略图或文件名弹出图片预览弹窗（读取本地相对路径 + `workDir` 拼接）
- [ ] **6.3** 实现"修改"按钮：弹出编辑弹窗，表单回显当前行数据
- [ ] **6.4** 实现修改保存逻辑：若修改了收款或支出金额，自动重新计算该笔及之后所有账目的结余，整体覆写 Excel 并刷新列表
- [ ] **6.5** 修改保存时同样显示 Loading 遮罩 + 异常处理，确保文件写入安全

---

## Phase 7: UI/UX 打磨

- [ ] **7.1** 统一错误提示样式（红字 toast/弹窗，如"请输入合法的金额"）
- [ ] **7.2** 统一成功提示样式（绿色 toast，如"保存成功"）
- [ ] **7.3** 表格列宽度适配，结余金额列置灰不可编辑样式
- [ ] **7.4** 大数量级（数千条）时确保加载和保存无白屏死机，必要时加入虚拟滚动或分页

---

## Phase 8: 测试验证

- [ ] **8.1** 确认单元测试 UT-001 ~ UT-004 全部通过
- [ ] **8.2** 确认集成测试 IT-001 ~ IT-004 全部通过
- [ ] **8.3** 手动测试异常场景：写入过程中强制关闭软件，重启后 `账本.xlsx` 未损坏
- [ ] **8.4** 手动测试数据迁移场景：将整个工作目录复制到另一台电脑，凭证图片仍能正常预览（相对路径验证）

---

## Phase 9: 打包与收尾

- [ ] **9.1** 配置 Tauri 打包参数（应用名称、图标、窗口大小）
- [ ] **9.2** 构建 Windows 安装包，验证安装和运行
- [ ] **9.3** 编写简易 README 说明安装与使用步骤
